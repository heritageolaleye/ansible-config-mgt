pipeline {
    agent any

    environment {
        ANSIBLE_HOST_KEY_CHECKING = 'False'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Ansible Playbook') {
            steps {
                sshagent(credentials: ['ansible-ssh-key']) {
                    sh '''
                      ansible-playbook \
                      -i inventory/hosts \
                      playbooks/site.yml
                    '''
                }
            }
        }
    }
}
